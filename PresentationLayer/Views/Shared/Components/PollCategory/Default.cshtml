@model List<DataAccessLayer.Entities.PollCategory> 
 

        <div class="row">
            <div class="col-lg-12">
                <ul id="portfolio-flters">
                    <li data-filter="*" class="filter-active">Tüm Anket Konuları</li>
                 @foreach (var item in Model)
                  {
                <li data-filter=".@item.Name">@item.Name</li>
                  }
                
              
                </ul>
            </div>
        </div>